# Portfolio detail site

This `docs/` directory is a static GitHub Pages source. `index.html` is the interactive project index; each `projects/*.html` page renders one complete repository inventory. The Three.js visual is intentionally confined to this Pages surface because GitHub profile README Markdown does not execute JavaScript.
